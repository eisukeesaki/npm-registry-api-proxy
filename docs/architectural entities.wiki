architectural entities
    origin API
        NPM public registry API
            resources
                /{package}
                /{package}/{version}
                /-/v1/search
                    parameters
                        text
                        size
                        from
                        quality
                        popularity
                        maintenance
    proxy API
        resources
            public
                /registeration
                    input
                        username
                    operations
                        sequence
                            API server
                                route handler
                                    create new User object
                                        generate API key
                                        init Usage.count
                                DB client
                                    send INSERT query to DB server
                            DB server
                                INSERT new User record
                                RETURNINGs INSERTed User record
                            API server
                                returns RETURNINGed User record
                    output
                        INSERTed User record                     
            protected
                proxied resources (of NPM Public Registry API)
                    /{packages}
                    /{packages}/{version}
                    /-/v1/search
                /usages
                    is
                        Usage records associated with a User
                            date
                            count
                /reqres
                    is
                        logs of
                            Request
                                time requested
                                HTTP request headers
                            Response
                                time responded
                                HTTP response headers
                                HTTP response body
    functionalities
        authN
            middleware
                        
                    
                        

